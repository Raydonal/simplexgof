% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplex_sim.R
\name{sim_table1}
\alias{sim_table1}
\title{Monte Carlo Size/Power Simulation for the Bootstrap U_n Test}
\usage{
sim_table1(
  n = 40,
  beta = c(-3, 2, 1, -1, 0.5),
  sigma2 = 0.5,
  R = 5000,
  B = 1000,
  alpha = c(0.01, 0.05, 0.1),
  mu_range = c("low", "mid", "high"),
  ncores = 1,
  seed = NULL
)
}
\arguments{
\item{n}{Sample size.}

\item{beta}{Numeric vector of mean-model coefficients.}

\item{sigma2}{Dispersion parameter (constant model).}

\item{R}{Number of Monte Carlo replications.}

\item{B}{Number of bootstrap replicates per replication.}

\item{alpha}{Significance levels.}

\item{mu_range}{One of \code{"low"}, \code{"mid"}, \code{"high"}; used to
select the covariate configuration that places fitted means near 0,
near 0.5, or near 1.}

\item{ncores}{Number of parallel workers for the outer MC loop. Default 1.}

\item{seed}{Random seed. Default \code{NULL}.}
}
\value{
A data frame with columns \code{alpha} and \code{rej_rate}.
}
\description{
Replicates the Monte Carlo study of Espinheira et al. (2026), computing
empirical rejection rates of the bootstrap \eqn{U_n} test under correct
specification (size) or misspecification (power).
}
\examples{
\donttest{
res <- sim_table1(n = 40, beta = c(-3, 2, 1, -1, 0.5),
                  sigma2 = 0.5, R = 100, B = 100,
                  mu_range = "mid", ncores = 1)
print(res)
}

}
