% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplex_plots.R
\name{plot_influence}
\alias{plot_influence}
\title{Influence Index Plot}
\usage{
plot_influence(
  x,
  threshold = 2,
  col.bar = "#2c7bb6",
  col.line = "#d7191c",
  label = TRUE,
  main = "Local influence -- case-weight perturbation",
  xlab = "Observation index",
  ylab = expression(C[It]),
  ...
)
}
\arguments{
\item{x}{A \code{"simplexfit"} object from \code{\link{simplex_fit}}, or a
list containing at least \code{$Cei} (influence vector of length \eqn{n})
as returned by \code{\link{simplex_diag}}.}

\item{threshold}{Numeric scalar; multiplier for the mean of \eqn{C_{I_t}}
used to draw the reference line.  Default 2.}

\item{col.bar}{Colour for the bars.  Default \code{"#2c7bb6"}.}

\item{col.line}{Colour for the reference line.  Default \code{"#d7191c"}.}

\item{label}{Logical; whether to label bars above the threshold with their
index.  Default \code{TRUE}.}

\item{main, xlab, ylab}{Plot title and axis labels.}

\item{...}{Additional arguments passed to \code{\link[graphics]{plot}}.}
}
\value{
Invisibly returns the numeric vector \eqn{C_{I_t}}.
}
\description{
Plots the total local-influence measure \eqn{C_{I_t}} for each observation
under case-weight perturbation, as used in the companion article
(Ospina et al., 2026, Figure 2).  A horizontal reference line at
\eqn{2 \bar{C}_I} flags potentially influential observations.
}
\examples{
data(ammonia)
X <- cbind(1, ammonia$corr_ar, ammonia$temp_agua,
           ammonia$corr_ar * ammonia$temp_agua)
Z <- cbind(1, ammonia$temp_agua,
           ammonia$corr_ar * ammonia$temp_agua)
fit <- simplex_fit(ammonia$perda, X, Z)
dg  <- simplex_diag(fit)
plot_influence(dg)

}
\seealso{
\code{\link{simplex_diag}}, \code{\link{simplex_fit}},
  \code{\link{plot.simplexfit}}
}
