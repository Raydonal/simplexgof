% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paper_figures.R
\name{simplex_Un_asymptotic}
\alias{simplex_Un_asymptotic}
\title{Asymptotic \eqn{U_n} Statistic (Finite-Difference Calibration)}
\usage{
simplex_Un_asymptotic(y, X, Z = NULL, eps = 1e-04)
}
\arguments{
\item{y}{Response vector in (0, 1).}

\item{X}{Design matrix for the mean sub-model.}

\item{Z}{Design matrix for the dispersion sub-model (or \code{NULL}).}

\item{eps}{Finite-difference step. Default \code{1e-4}.}
}
\value{
Scalar \eqn{U_n} (asymptotic calibration), or \code{NA} if the
  model fails to converge.
}
\description{
Computes the \eqn{U_n} statistic using the finite-difference gradient
\eqn{J}, which gives the correct asymptotic variance for the simplex
class.  This is the version whose null distribution is studied in the
simulation section of the companion paper (Figure 1).
}
\details{
For the bootstrap test, use \code{\link{simplex_gof}} instead --- the
analytic gradient is bootstrap-invariant and faster.
}
\examples{
set.seed(1)
n  <- 40
X  <- cbind(1, matrix(runif(n * 4), n, 4))
mu <- plogis(drop(X \%*\% c(2, -0.5, -1.4, 1.25, -2.35)))
y  <- rsimplex(n, mu, 0.5)
Un <- simplex_Un_asymptotic(y, X)
Un

}
\seealso{
\code{\link{simplex_gof}}, \code{\link{simplex_diag}}
}
